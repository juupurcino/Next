import Link from "next/link";

interface IFooter {
    op1: string;
    op2: number;
    op3: boolean;
}

export const Footer: React.FC<IFooter> = ({op1, op2, op3}) => {

    return(
        
        <footer>

        </footer>
    
    )

}